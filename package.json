{
  "name": "les-mots-a-vous",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/generator.js && node --check dist/pdf.js"
  },
  "dependencies": {
    "jspdf": "^4.2.1"
  }
}
